[workspace]
resolver = "2"
members = [
    "escrow",
    "reputation",
    "driver_identity",
]

[patch.crates-io]
ethnum = { path = "ethnum-patch" }
arbitrary = { path = "arbitrary-patch" }
derive_arbitrary = { path = "derive_arbitrary-patch" }
